export enum Permissions {
    ALL = "*",
    PUBLISH = "publish",
    READ= "read",
    PLAYBACK = "playback",
    API = "api",
    METRICS = "metrics",
    PPROF = "pprof",
    CREATE_USER = "create_user",
}